import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

// Placeholder: invite flow and account/role management screens are
// S4-AUTH-005's job.
export default function AccountsPage() {
  return (
    <Card className="glass-panel border-none">
      <CardHeader>
        <CardTitle className="font-heading text-xl">Accounts</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">Coming in S4-AUTH-005.</CardContent>
    </Card>
  );
}
