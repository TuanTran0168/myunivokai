# Myunivokai Web

Next.js frontend for the Myunivokai MVP.

```bash
npm install
npm run dev
```

The app reads `NEXT_PUBLIC_API_BASE_URL` from `.env.local` and defaults to `http://localhost:8080/api/v1`.
